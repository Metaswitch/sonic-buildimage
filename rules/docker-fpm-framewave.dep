
DPATH       := $($(DOCKER_FPM_FRAMEWAVE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-fpm-framewave.mk rules/docker-fpm-framewave.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_FPM_FRAMEWAVE)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_FPM_FRAMEWAVE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_FPM_FRAMEWAVE)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_FPM_FRAMEWAVE),$(DOCKER_FPM_FRAMEWAVE_DBG)))