
DPATH       := $($(DOCKER_SWAN_AGENT)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-swan-agent.mk rules/docker-swan-agent.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_SWAN_AGENT)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_SWAN_AGENT)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_SWAN_AGENT)_DEP_FILES   := $(DEP_FILES)
