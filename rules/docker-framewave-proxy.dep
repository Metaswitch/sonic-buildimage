
DPATH       := $($(DOCKER_FRAMEWAVE_PROXY)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-framewave-proxy.mk rules/docker-framewave-proxy.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_FRAMEWAVE_PROXY)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_FRAMEWAVE_PROXY)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_FRAMEWAVE_PROXY)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_FRAMEWAVE_PROXY),$(DOCKER_FRAMEWAVE_PROXY_DBG)))