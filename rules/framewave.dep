
SPATH       := $($(FRAMEWAVE)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/framewave.mk rules/framewave.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
SMDEP_FILES := $(addprefix $(SPATH)/,$(shell cd $(SPATH) && git ls-files))

$(FRAMEWAVE)_CACHE_MODE  := GIT_CONTENT_SHA
$(FRAMEWAVE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(FRAMEWAVE)_DEP_FILES   := $(DEP_FILES)

